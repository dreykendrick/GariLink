# Railway deployment configuration
# Root Directory must be set to "backend" in Railway Settings → Source

[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npm run build && ls -la dist/src/"

[deploy]
startCommand = "node dist/src/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
